fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pix2text==1.1.2.2
Pillow==10.4.0
pdf2image==1.17.0
pytest==8.3.2
pytest-asyncio==0.24.0
httpx==0.27.2
PyMuPDF==1.28.0
# WHY this one is in the LEAN requirements.txt, unlike paddlepaddle/paddleocr
# (see requirements-fallback.txt): wordninja is pure Python with a small
# bundled word-frequency file, no heavy ML runtime -- cheap enough that
# test_inference.py can exercise inference.py's _fix_missing_spaces() for
# real in CI, instead of needing to mock it like the PaddleOCR call itself.
wordninja==2.0.0
# pyspellchecker: same reasoning as wordninja above -- lightweight, pure
# Python, no heavy ML runtime, so inference.py's _fix_typos() gets a real
# (not mocked) test in CI too.
pyspellchecker==0.8.1
